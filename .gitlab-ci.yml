image: openjdk:21.0.1

stages:
  - build
  - test
  - deploy

build:
  stage: build
  script:
    -javac -d src/ca/uqam/info/mgl7460/boutique/bin

test:
  stage: test
    - echo "test"

deploy:
  stage: deploy
    -echo "test"